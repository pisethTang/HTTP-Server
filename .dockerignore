# Don't copy the compiled binary (we'll rebuild it)
server

# Don't copy git files
.git
.gitignore

# Don't copy uploaded data
uploaded_data.txt

# Don't copy README (optional, but saves space)
*.md

# Don't copy local Node.js dependencies or build output (we'll rebuild them)
node_modules
dist
